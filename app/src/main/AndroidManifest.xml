<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adminportatil.cebanc_tortillas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
/////////////Splash Activity que se ejecuta primero///////////////////
        <activity android:name=".Splash"
            android:screenOrientation="portrait">
            <intent-filter>

                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".DatosCliente" />
        <activity android:name=".OpcionesTortilla"/>
        <activity android:name=".Bebidas" />
        <activity android:name=".Factura" />

    </application>
</manifest>
